/**
 * GemGym — UI Components Barrel Export
 *
 * Import all UI primitives from this single path.
 * Example: import { Button, Input, Card } from "@/components/ui"
 */

export { Button } from "./Button";
export type { ButtonVariant, ButtonSize } from "./Button";

export { Spinner } from "./Spinner";

export { Input } from "./Input";

export {
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  CardFooter,
} from "./Card";

export { Badge, MemberStatusBadge, SubscriptionStatusBadge, PaymentStatusBadge, HardwareStatusBadge } from "./Badge";

export {
  Dialog,
  DialogTrigger,
  DialogClose,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogBody,
  DialogFooter,
} from "./Dialog";

export { DataTable } from "./DataTable";
export type { Column } from "./DataTable";
